name: new_projectes
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  curved_navigation_bar: ^1.0.6
  google_nav_bar: ^5.0.7
  line_icons: ^2.0.3
  image_picker: ^1.1.2
  http: ^1.3.0
  intl: ^0.20.2
  dio: ^5.8.0+1
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  icons_launcher: ^3.0.0
icons_launcher:
  image_path: "assets/images/flamingo-icon-0.jpg"
  platforms:
    android:
      enable: true
    ios:
      enable: true

flutter:

  
  uses-material-design: true

  
  assets:
    - assets/images/
  